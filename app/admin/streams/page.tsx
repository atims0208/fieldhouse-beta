"use client"

import React from 'react';

export default function ManageStreamsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-fhsb-cream">Manage Streams</h1>
      <p className="text-muted-foreground">Stream overview table and actions (like stopping streams) will go here.</p>
      {/* TODO: Implement stream fetching, table display, stop stream actions */}
    </div>
  );
} 